### Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

### 虚拟环境
.venv
venv/
ENV/
.python-version

### 测试 / 覆盖率 / lint 与类型缓存
htmlcov/
.tox/
.coverage
.coverage.*
.cache
coverage.xml
*.cover
.hypothesis/
.pytest_cache/
.ruff_cache/
.mypy_cache/

### Django
# .mo 由 compilemessages 在部署时生成，仓库只保留 locale 下的 .po 源文件。
*.mo
*.pot
staticfiles/
xcash/media/
celerybeat-schedule*
dump.rdb

### 环境变量与密钥
.env
.env.local
.env.signer
.env_mainnet
**/.env_mainnet
**/.env.production
.envs/*

### 运维敏感产物
# upgrade.sh 生成的升级前数据库备份（含生产数据），绝不可入库。
backups/
scripts/merchant_hmac/
business-plan/
xcash/tron/mainnet_deployment/
xcash/tron/nile_verification/

### Foundry 合约编译产物
# xcash/evm/contracts 由 Foundry 自带的局部 .gitignore 覆盖 out/ 与 cache/；
# xcash/tron/contracts 没有局部 .gitignore，需在此显式声明。
xcash/tron/contracts/cache/
xcash/tron/contracts/out/

### 前端（pay-fronted）
# 根目录 docker-compose.yml 作为公开自托管入口，需要纳入版本控制。
# pay-fronted/dist 被上方 dist/ 命中：构建产物经 scripts/build-pay-fronted.sh
# 同步到 xcash/static/pay 后才入库。
node_modules/
dist-ssr/
.eslintcache
.npm
logs/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

### AI 工具
.claude/*
.worktrees/
.superpowers/
**/CLAUDE.md
# .claude/CLAUDE.MD 是指向 ../AGENTS.md 的符号链接，供只认 CLAUDE.md 的工具使用。
# 这条白名单必须排在 **/CLAUDE.md 之后：gitignore 后置规则覆盖前置规则，且在
# core.ignorecase 为 true 的文件系统（macOS）上 **/CLAUDE.md 会连大写 .MD 一并命中。
!.claude/CLAUDE.MD
**/GEMINI.md
**/prompt.md
**/docs/superpowers/
docs/
*.local

### 编辑器 / IDE
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace
.ipython/
*.sw?
*~

### 操作系统
.DS_Store
Thumbs.db
Desktop.ini
